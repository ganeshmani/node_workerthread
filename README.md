## Generating Image thumbnail using Node Worker Threads

It Generates the Image thumbnail by spawning different worker threads.

### Libraries Used
  - worker_threads
  - imagemagick

### To Run the Code
```
npm install

node --experimental-worker main.js

```